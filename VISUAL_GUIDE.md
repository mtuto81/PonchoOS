# PonchoOS Build Pipeline - Visual Guide

## 📦 System Architecture

```
┌─────────────────────────────────────────────────────────┐
│                    PonchoOS Project                      │
├─────────────────────────────────────────────────────────┤
│                                                           │
│  ┌──────────────┐   ┌──────────────┐   ┌──────────────┐ │
│  │  kernel/src  │   │   gnu-efi/   │   │  OVMFbin/    │ │
│  │ C++ & ASM    │   │ Bootloader   │   │ Firmware     │ │
│  └──────┬───────┘   └──────┬───────┘   └──────┬───────┘ │
│         │                  │                  │          │
│         └──────────────────┼──────────────────┘          │
│                            │                             │
│                      ┌─────▼──────┐                      │
│                      │ build.sh   │                      │
│                      │ (Pipeline) │                      │
│                      └─────┬──────┘                      │
│                            │                             │
│          ┌─────────────────┼──────────────────┐          │
│          │                 │                  │          │
│    ┌─────▼──────┐   ┌──────▼──────┐   ┌──────▼──────┐   │
│    │ Compiler   │   │  Assembler  │   │  Linker    │   │
│    │ (GCC)      │   │  (NASM)     │   │  (LD)      │   │
│    └─────┬──────┘   └──────┬──────┘   └──────┬──────┘   │
│          │                 │                  │          │
│          └─────────────────┼──────────────────┘          │
│                            │                             │
│                      ┌─────▼──────────────┐              │
│                      │  kernel/lib/       │              │
│                      │ Object Files (.o)  │              │
│                      └─────┬──────────────┘              │
│                            │                             │
│                      ┌─────▼────────────┐               │
│                      │ kernel/bin/      │               │
│                      │ kernel.elf       │               │
│                      └─────┬────────────┘               │
│                            │                             │
│          ┌─────────────────┼──────────────────┐          │
│          │                 │                  │          │
│    ┌─────▼──────┐   ┌──────▼──────┐   ┌──────▼──────┐   │
│    │ FAT Image  │   │ ISO Image   │   │ Startup    │   │
│    │ Creator    │   │ Creator     │   │ Files      │   │
│    └─────┬──────┘   └──────┬──────┘   └──────┬──────┘   │
│          │                 │                  │          │
│          └─────────────────┼──────────────────┘          │
│                            │                             │
│                      ┌─────▼────────────────┐            │
│                      │  Bootable Image      │            │
│                      │ (FAT or ISO)         │            │
│                      └─────┬────────────────┘            │
│                            │                             │
│                      ┌─────▼────────────────┐            │
│                      │  QEMU Emulator       │            │
│                      │  (qemu-system-x86_64)│            │
│                      └─────┬────────────────┘            │
│                            │                             │
│                      ┌─────▼────────────────┐            │
│                      │  Running OS          │            │
│                      │  on UEFI Firmware    │            │
│                      └──────────────────────┘            │
│                                                           │
└─────────────────────────────────────────────────────────┘
```

## 🔄 Build Workflow

### Standard Build Flow
```
./build.sh
    │
    ├─► Check Dependencies ──► Verify Tools Installed
    │
    ├─► Clean Previous Build ──► Remove old artifacts
    │
    ├─► Compile Kernel
    │   ├─► Compile C++ sources (*.cpp → *.o)
    │   ├─► Compile ASM sources (*.asm → *_asm.o)
    │   └─► Link objects (link.sh)
    │
    ├─► Create Image
    │   ├─► Create FAT12 floppy image
    │   ├─► Copy bootloader (main.efi)
    │   ├─► Copy kernel (kernel.elf)
    │   └─► Copy startup script
    │
    └─► Done ✅
        Image: kernel/bin/CustomOS.img
```

### Build and Run Flow
```
./build.sh --run
    │
    ├─► [Standard Build Flow] ──► See above
    │
    ├─► Launch QEMU
    │   ├─► Load OVMF firmware
    │   ├─► Mount bootable image
    │   ├─► Initialize CPU (qemu64)
    │   ├─► Allocate memory (256MB)
    │   └─► Start emulation
    │
    └─► OS Runs in QEMU ✅
```

### Debug Flow
```
./build.sh --debug --run
    │
    ├─► [Standard Build Flow]
    │
    └─► Launch QEMU with Debug Output
        ├─► Enable interrupt tracing (-d int)
        ├─► Disable auto-reboot (-no-reboot)
        ├─► Redirect serial to stdout (-serial stdio)
        └─► Full execution visibility ✅
```

## 🧩 File Structure After Build

```
PonchoOS/
├── build.sh                        ← Main pipeline (NEW)
├── debug.sh                        ← Debugging tool (NEW)
├── build.config                    ← Config (NEW)
├── BUILD_PIPELINE.md               ← Documentation (NEW)
├── QUICK_REFERENCE.md              ← Quick guide (NEW)
├── SETUP_COMPLETE.md               ← This summary (NEW)
│
├── kernel/
│   ├── Makefile                    ← Enhanced (MODIFIED)
│   ├── kernel.ld                   ← Linker script
│   ├── startup.nsh                 ← Boot script
│   │
│   ├── src/                        ← Source code
│   │   ├── kernel.cpp
│   │   ├── interrupts/
│   │   ├── memory/
│   │   └── ... (other sources)
│   │
│   ├── lib/                        ← Object files (BUILD)
│   │   ├── kernel.o
│   │   ├── interrupts_asm.o
│   │   └── ...
│   │
│   └── bin/                        ← Build output (BUILD)
│       ├── kernel.elf              ← Compiled kernel
│       └── CustomOS.img            ← Bootable image
│
├── gnu-efi/
│   ├── x86_64/
│   │   └── bootloader/
│   │       └── main.efi            ← EFI bootloader
│   └── ... (headers, libraries)
│
├── OVMFbin/
│   ├── OVMF_CODE-pure-efi.fd       ← UEFI firmware code
│   └── OVMF_VARS-pure-efi.fd       ← UEFI firmware variables
│
└── .vscode/
    └── tasks.json                  ← IDE tasks (NEW)
```

## 🎮 VS Code Integration

```
┌─────────────────────────────────────────────┐
│         VS Code Command Palette             │
│  Ctrl+Shift+P → Type "Tasks: Run Task"      │
└────────────────┬────────────────────────────┘
                 │
        ┌────────┴────────┐
        │                 │
┌───────▼──────────┐  ┌───▼────────────────┐
│ Build Tasks      │  │ Run/Debug Tasks    │
├──────────────────┤  ├────────────────────┤
│ • Build Kernel   │  │ • Run QEMU         │
│ • Build All      │  │ • Run (Debug)      │
│ • Full Rebuild   │  │ • Build & Run      │
│ • Clean          │  │ • Build & Debug    │
│ • Show Help      │  │ • Debug with GDB   │
└──────────────────┘  │ • Trace Execution  │
                      │ • Memory Trace     │
                      └────────────────────┘
```

## 🔧 Tool Dependency Graph

```
┌─────────────────────────────────────────────┐
│         Required Tools                      │
├─────────────────────────────────────────────┤
│ ✅ gcc          ─── Compile C++ code        │
│ ✅ nasm         ─── Assemble ASM code       │
│ ✅ ld           ─── Link object files       │
│ ✅ make         ─── Run build rules         │
│ ✅ qemu-system- ─── Run emulation           │
│    x86_64                                   │
└─────────────────────────────────────────────┘

┌─────────────────────────────────────────────┐
│      Optional Tools (Enhanced Features)     │
├─────────────────────────────────────────────┤
│ 📦 mtools       ─── FAT image support       │
│ 📦 grub-mkrescue── ISO image support       │
│ 📦 gdb          ─── Remote debugging        │
│ 📦 xorriso      ─── Advanced ISO ops        │
└─────────────────────────────────────────────┘
```

## 📊 Build Time Estimates

```
Activity                    Time
─────────────────────────────────
Check Dependencies          ~0.5s
Compile Kernel             ~2-5s
  ├─ C++ sources           ~2-3s
  └─ Assembly              ~0.5-1s
Link Objects               ~0.5-1s
Create FAT Image           ~1-2s
Total Build                ≈ 5-10s
───────────────────────────────────
Launch QEMU                ~1-2s
Boot OS                    ~2-5s
───────────────────────────────────
Full Cycle (Build + Run)   ≈ 10-20s
```

## 🎯 Command Decision Tree

```
┌─ Want to just build?
│  └─ ./build.sh
│
├─ Want to build and test?
│  └─ ./build.sh --run
│
├─ Something went wrong, rebuild?
│  └─ ./build.sh --clean --run
│
├─ Need detailed output?
│  └─ ./build.sh --verbose --run
│
├─ Want GDB debugging?
│  └─ ./debug.sh gdb
│     (in another terminal: gdb kernel/bin/kernel.elf)
│     (gdb) target remote localhost:1234
│
├─ Need execution trace?
│  └─ ./debug.sh trace
│
├─ Want to analyze memory?
│  └─ ./debug.sh memory
│
└─ Use make directly?
   └─ cd kernel && make [target]
      ├─ make kernel      (compile only)
      ├─ make all         (compile + image)
      ├─ make run         (standard QEMU)
      ├─ make run-debug   (debug output)
      └─ make clean       (remove artifacts)
```

## 🐛 Debug Modes Comparison

```
Mode          GDB    Monitor   Trace    Memory   Interactive
──────────────────────────────────────────────────────────────
External GDB   ✅      ❌       ❌       ❌         ❌
CLI Monitor    ❌      ✅       ❌       ❌         ✅
Inst. Trace    ❌      ❌       ✅       ❌         ❌
Memory Track   ❌      ❌       ❌       ✅         ❌
Full Debug     ❌      ❌       ❌       ❌         ✅
Ease of Use   ⭐⭐⭐   ⭐⭐    ⭐⭐     ⭐⭐      ⭐⭐⭐
Power         ⭐⭐⭐⭐  ⭐⭐⭐   ⭐⭐    ⭐⭐      ⭐⭐⭐
```

## 💾 Image Format Comparison

```
Format      Size      Boot    Tools         Use Case
──────────────────────────────────────────────────────
FAT         1.44MB    UEFI    mtools        Quick test
ISO         Larger    Legacy  grub-mkrescue CD/USB burn
──────────────────────────────────────────────────────
Default: FAT (faster creation and testing)
```

## 🚀 Quick Reference Card

```
┌────────────────────────────────────────────┐
│    PonchoOS Build Pipeline - Cheatsheet    │
├────────────────────────────────────────────┤
│ Build:        ./build.sh                   │
│ Build & Run:  ./build.sh --run             │
│ Debug:        ./build.sh --debug --run     │
│ GDB Mode:     ./debug.sh gdb               │
│ Clean:        ./build.sh --clean           │
│ Help:         ./build.sh help              │
│              (or) make -C kernel help      │
├────────────────────────────────────────────┤
│ In QEMU:                                   │
│ • Ctrl+Alt+1  Console                      │
│ • Ctrl+Alt+2  Monitor                      │
│ • Ctrl+Alt+G  Grab/release mouse           │
│ • Ctrl+C      Pause/resume                 │
├────────────────────────────────────────────┤
│ VS Code (Ctrl+Shift+B):                    │
│ • Select "PonchoOS: Build & Run"           │
└────────────────────────────────────────────┘
```

---

**For more details, see:**
- `BUILD_PIPELINE.md` - Complete documentation
- `QUICK_REFERENCE.md` - Command reference
- `SETUP_COMPLETE.md` - Setup summary
